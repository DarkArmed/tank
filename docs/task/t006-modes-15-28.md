# t006 15→28 玩法组

对应需求：p001 第 14 节

技术设计：开始实施前必须修订 `docs/DESIGN.md`

状态：暂缓，未分配

## 目标

在当前 `01→14` 里程碑稳定后，一次性加入 `15→28` 玩法组、SELECT 切组和敌方拾取道具。

## 范围

- 以 p001 后续确认的需求启用第二组 TANK A～N。
- 实现玩法选择页在 `01→14` 与 `15→28` 之间切换。
- 实现敌方拾取八种道具及其玩家可见结果。
- 补充第二组与第一组的其它已确认差异。
- 更新相关自动测试和端到端验证。

## 不包含

- 在当前需求未补全时猜测敌方道具效果或模式差异。
- 建造模式。
- 未经确认的新地图或其它烟山版本。
- 在更新 DESIGN 前修改实现代码。

## 依赖

- 当前 `01→14` 里程碑完成。
- p001 第 14 节补全全部敌方道具与第二组差异，并由用户确认。
- 根据确认后的需求先修订 `docs/DESIGN.md`，冻结新增状态、接口和集成方式。
- 用户明确分配本 Task。

## 涉及模块

具体写入范围必须在需求和 DESIGN 更新后确定；本 Task 当前不授予任何代码目录修改权限。

## 验收标准

- `15→28` 的需求不存在未确认的玩家行为、数值或胜负规则。
- SELECT 可以切换两组，A～N 与各自编号映射正确。
- 两组的规则差异和敌方八种道具效果均有针对性测试。
- 当前 `01→14` 行为不回归。
- PRD、DESIGN、Task、代码和 `ARCHITECTURE.md` 保持一致。

## 验证方式

待 p001 与 DESIGN 补全后，在本 Task 中明确 Targeted Tests、端到端流程和用户实机验证步骤；当前不得用未确认方案预写验收结果。
