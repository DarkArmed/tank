# AGENTS.md

## 1. 技术设计标准

- `docs/DESIGN.md` 是项目唯一的规范性技术设计标准。
- PRD 是需求标准：玩家可见行为、玩法规则、数值、体验约束和验收结果只能在 PRD 中定义。
- DESIGN 是技术设计标准：技术栈、模块边界、公共接口、数据结构、实现机制和集成方式只能在 `DESIGN.md` 中定义。
- DESIGN 不得为了补全实现而新增玩法行为、数值、操作语义或验收标准；此类内容必须回到 PRD 并由用户确认。
- Task 只定义工作范围、依赖和验证，不得形成另一套需求或技术设计。
- 不影响需求和跨模块契约的局部代码选择属于实现细节，不升级为项目标准。
- `ARCHITECTURE.md` 只描述当前已实现架构，ADR 只记录决策背景；二者均不得替代 `DESIGN.md`。
- 发现实现需要改变技术设计时，先更新 `DESIGN.md` 并等待用户确认，再修改代码。

---

## 2. Python 环境

必须使用项目根目录的 `.venv/`。

- `.venv/` 不存在时首先创建。
- 除创建 `.venv/` 外，禁止使用系统 Python。
- 禁止使用系统 `pip` 或全局安装依赖。
- Python、pip、uvicorn、pytest、ruff、mypy 等必须通过 `.venv/` 执行。
- 根据操作系统使用 `.venv/bin/` 或 `.venv/Scripts/`。
- `.venv/` 必须加入 `.gitignore`。
- Python 依赖记录在 `backend/requirements.txt`。
- 不得依赖系统 Python 中已有的第三方包。

---

## 3. 项目结构

默认结构：

project/
├── .venv/
├── frontend/
├── backend/
├── docs/
│   ├── prd/
│   ├── adr/
│   ├── task/
│   ├── DESIGN.md
│   └── ARCHITECTURE.md
├── .gitignore
└── AGENTS.md

从最小结构开始，复杂度确实增加后再拆分模块。

---

## 4. 文档规范

`docs/prd/`
- 描述玩家可见行为、玩法规则、数值、用户流程、功能范围、体验约束和验收标准。
- 只回答“做什么”和“做到什么程度”，不得出现模块、接口、Schema、算法或代码组织。
- 编号：`p` + 三位数字，从 `p001` 起，只作文件名前缀。
- 文件名：`p001-{summary}.md`。`summary` 用简短英文短横线，说明主题。
- 标题以编号开头，例如 `# p001 烟山90坦克复刻`。
- 示例：`docs/prd/p001-yanshan-90-tank.md`。

`docs/task/`
- 记录 Task 的目标、范围、不包含内容、依赖、涉及模块、验收标准和验证方式。
- Task 应尽可能独立，减少多个 Agent 之间的修改冲突。
- 编号：`t` + 三位数字，从 `t001` 起，只作文件名前缀。
- 文件名：`t001-{summary}.md`。`summary` 用简短英文短横线，说明主题。
- 标题以编号开头，例如 `# t001 对战逻辑`。
- 正文中引用其它文档时使用编号（`p001`、`t002`）；指向文件时用完整文件名。
- 示例：`docs/task/t001-sim.md`。

`docs/adr/`
- 记录重要且长期有效的架构或技术决策及原因。
- 局部、简单、容易撤销的决定不创建 ADR。
- ADR 中当前有效的技术结论必须同步进入 `DESIGN.md`。

`docs/DESIGN.md`
- 项目唯一的规范性技术设计标准，描述“必须如何实现”。
- 包括技术栈、模块职责、依赖方向、公共接口、数据模型、运行机制和集成规则。
- 不定义玩家可见行为、玩法数值或验收标准；发现需求缺口时返回 PRD，不在设计中补需求。
- PRD、Task、ADR、代码注释和聊天记录不得重复定义或覆盖其中的标准。
- 技术设计变更必须先更新本文档并经用户确认。

`docs/ARCHITECTURE.md`
- 描述系统当前真实架构，而非修改历史。
- 包括主要模块、职责边界、数据流、API、数据模型、持久化方式和关键依赖。
- 任何影响架构的代码修改必须同步更新。
- 代码与 `ARCHITECTURE.md` 必须保持一致。
- 它是描述性文档，不是技术设计标准；目标设计以 `DESIGN.md` 为准。

---

## 5. 核心原则

优先级：

1. 正确性
2. 核心流程端到端可运行
3. 简单清晰
4. 开发效率
5. 用户体验
6. 可维护性

尽早得到可运行版本，再增量完善。

避免：
- 过度设计和过早抽象
- 推测未来需求
- 不必要的依赖和基础设施
- 为“最佳实践”增加无实际价值的层级

简单方案能够满足需求时，不主动复杂化。

---

## 6. 工作流程

### 规划

1. 明确需求、核心用户流程和验收标准。
2. 创建或更新对应 PRD。
3. 创建或更新 `docs/DESIGN.md`，集中形成唯一技术设计。
4. 在 `docs/task/` 中拆分 Task。
5. 明确 Task 的范围、边界、依赖、验收标准和验证方式；Task 只引用、不复制技术设计。
6. 优先设计可独立实施的 Task，减少 Agent 间冲突。
7. 完成 PRD、DESIGN 和 Task 后停止，等待用户确认。
8. 由用户确认技术设计与 Task 拆分，并决定 Task 如何分配。
9. 未经用户明确分配，不得自行开始实施 Task。

### 实施

1. 只实施用户明确分配的 Task。
2. 阅读对应 PRD、`docs/DESIGN.md`、Task 和已有的 `docs/ARCHITECTURE.md`。
3. 遵守 Task 边界，不主动修改其他 Task 的职责范围。
4. 如发现必须跨 Task 修改或偏离 `DESIGN.md`，先告知用户；设计变化必须先修订设计文档并等待确认。
5. 尽早实现最小可工作的功能并持续验证。
6. 完成后更新 Task；如影响架构，同步更新 `ARCHITECTURE.md`。
7. 随后进入验证流程。

---

## 7. 实现约定

- 严格按 `docs/DESIGN.md` 实施，不在代码中引入未记录的技术标准。
- 类型保持明确、简单，避免 `any` 和不必要的抽象层。
- 不为“最佳实践”拆分大量简单模块，不为未来需求预建基础设施。
- 需求存在轻微歧义且不影响技术设计时，采用常见、合理的默认方案继续推进。
- 未写入 PRD 的玩法默认值只能作为可调整的实现值，不能写入 DESIGN 变成需求标准。
- 歧义影响玩家行为、胜负、数值或验收时先更新 PRD；问题影响公共接口、数据模型、模块边界或运行机制时先更新 DESIGN。两者均等待用户确认。

---

## 8. 验证工具

统一使用以下工具，不自行替换同类工具。

前端：
- TypeScript Compiler：类型检查
- Vitest：单元测试和必要的集成测试

后端：
- pytest：单元测试和必要的集成测试
- Ruff：Lint 和代码质量检查
- mypy：静态类型检查

规则：
- 为重要业务逻辑和关键边界情况编写必要测试。
- 优先执行与当前 Task 相关的 Targeted Tests。
- Task 完成前必须执行相关测试和静态检查。
- 检查失败时必须修复并重新验证。
- 禁止通过跳过测试、关闭规则或降低检查标准规避失败。

---

## 9. 验证流程

Task 实施完成后：

1. Agent 先自动验证：
   - 启动相关服务；
   - 执行必要的 Type Check、Lint 和 Targeted Tests；
   - 验证相关 API、核心流程和重要错误路径；
   - 修复发现的问题并重新验证。

2. 自动验证通过后停止修改，并向用户汇报：
   - 已执行的验证及结果；
   - 已知限制；
   - 建议的手动验证步骤。

3. 等待用户进行手动验证。

4. 用户确认前不得将 Task 标记为最终完成。

5. 用户发现问题时，根据反馈修改，并重新执行自动验证 → 用户验证流程。

---

## 10. 完成标准

最终完成前必须确认：

- `DESIGN.md` 要求的项目与服务均可正常启动。
- Frontend 正常启动；仅当设计包含 Backend 时才要求 `.venv/` 和后端验证。
- 仅当设计包含 API 时验证核心 API 实际调用。
- 核心用户流程端到端实际运行成功。
- Type Check、Lint 和必要测试通过。
- 无明显 Runtime Error。
- 删除 Debug Code 和无用代码。
- PRD / DESIGN / Task 与最终实现一致。
- `ARCHITECTURE.md` 与当前代码一致。
- 用户已完成最终手动验证。

最后只需简短总结：
- 实现了什么
- 如何启动
- 自动验证结果
- 关键技术决策
- 重要假设或已知限制
