# AGENTS.md

## 1. 技术栈

除非需求明确要求，否则固定使用：

前端：
- TypeScript + React + Vite
- 函数组件 + React Hooks
- Fetch API

后端：
- Python 3 + FastAPI
- Pydantic + Uvicorn

架构：
- 前后端分离：`frontend/` + `backend/`
- 前后端通过 REST API 通信
- 不随意替换核心技术栈或引入大型框架

---

## 2. Python 环境

必须使用项目根目录的 `.venv/`。

- `.venv/` 不存在时首先创建。
- 除创建 `.venv/` 外，禁止使用系统 Python。
- 禁止使用系统 `pip` 或全局安装依赖。
- Python、pip、uvicorn、pytest、ruff、mypy 等必须通过 `.venv/` 执行。
- 根据操作系统使用 `.venv/bin/` 或 `.venv/Scripts/`。
- `.venv/` 必须加入 `.gitignore`。
- Python 依赖记录在 `backend/requirements.txt`。
- 不得依赖系统 Python 中已有的第三方包。

---

## 3. 项目结构

默认结构：

project/
├── .venv/
├── frontend/
├── backend/
├── docs/
│   ├── prd/
│   ├── adr/
│   ├── task/
│   └── ARCHITECTURE.md
├── .gitignore
└── AGENTS.md

从最小结构开始，复杂度确实增加后再拆分模块。

---

## 4. 文档规范

`docs/prd/`
- 描述需求、用户流程、功能范围和验收标准。
- 关注“做什么”，避免实现细节。

`docs/task/`
- 记录 Task 的目标、范围、不包含内容、依赖、涉及模块、验收标准和验证方式。
- Task 应尽可能独立，减少多个 Agent 之间的修改冲突。

`docs/adr/`
- 记录重要且长期有效的架构或技术决策及原因。
- 局部、简单、容易撤销的决定不创建 ADR。

`docs/ARCHITECTURE.md`
- 描述系统当前真实架构，而非修改历史。
- 包括主要模块、职责边界、数据流、API、数据模型、持久化方式和关键依赖。
- 任何影响架构的代码修改必须同步更新。
- 代码与 `ARCHITECTURE.md` 必须保持一致。

---

## 5. 核心原则

优先级：

1. 正确性
2. 核心流程端到端可运行
3. 简单清晰
4. 开发效率
5. 用户体验
6. 可维护性

尽早得到可运行版本，再增量完善。

避免：
- 过度设计和过早抽象
- 推测未来需求
- 不必要的依赖和基础设施
- 为“最佳实践”增加无实际价值的层级

简单方案能够满足需求时，不主动复杂化。

---

## 6. 工作流程

### 规划

1. 明确需求、核心用户流程和验收标准。
2. 创建或更新对应 PRD。
3. 在 `docs/task/` 中拆分 Task。
4. 明确 Task 的范围、边界、依赖、验收标准和验证方式。
5. 优先设计可独立实施的 Task，减少 Agent 间冲突。
6. 完成 PRD 和 Task 后停止，等待用户确认。
7. 由用户确认 Task 拆分并决定 Task 如何分配。
8. 未经用户明确分配，不得自行开始实施 Task。

### 实施

1. 只实施用户明确分配的 Task。
2. 阅读对应 PRD、Task 和 `docs/ARCHITECTURE.md`。
3. 遵守 Task 边界，不主动修改其他 Task 的职责范围。
4. 如发现必须跨 Task 修改，先告知用户。
5. 尽早实现最小可工作的功能并持续验证。
6. 完成后更新 Task；如影响架构，同步更新 `ARCHITECTURE.md`。
7. 随后进入验证流程。

---

## 7. 实现约定

前端：
- 默认使用 React Hooks 和局部状态。
- 非必要不引入全局状态管理。
- 默认使用 Fetch API。
- TypeScript 类型保持明确、简单，避免 `any`。
- 不为组件化而拆分大量简单组件。

后端：
- FastAPI 项目结构从简单开始。
- 逻辑较少时可以集中在 `main.py`。
- 复杂度实际增加后再拆分。
- 使用 Pydantic 定义请求、响应和数据验证。

数据：
- 无持久化要求时默认使用内存数据。
- 需要简单持久化时优先 SQLite。
- 非必要不引入 PostgreSQL、Redis、ORM、Migration 等基础设施。

需求存在轻微歧义时，采用常见、合理的默认方案继续推进。
只有歧义明显影响核心功能、数据模型或架构时才询问用户。

---

## 8. 验证工具

统一使用以下工具，不自行替换同类工具。

前端：
- TypeScript Compiler：类型检查
- Vitest：单元测试和必要的集成测试

后端：
- pytest：单元测试和必要的集成测试
- Ruff：Lint 和代码质量检查
- mypy：静态类型检查

规则：
- 为重要业务逻辑和关键边界情况编写必要测试。
- 优先执行与当前 Task 相关的 Targeted Tests。
- Task 完成前必须执行相关测试和静态检查。
- 检查失败时必须修复并重新验证。
- 禁止通过跳过测试、关闭规则或降低检查标准规避失败。

---

## 9. 验证流程

Task 实施完成后：

1. Agent 先自动验证：
   - 启动相关服务；
   - 执行必要的 Type Check、Lint 和 Targeted Tests；
   - 验证相关 API、核心流程和重要错误路径；
   - 修复发现的问题并重新验证。

2. 自动验证通过后停止修改，并向用户汇报：
   - 已执行的验证及结果；
   - 已知限制；
   - 建议的手动验证步骤。

3. 等待用户进行手动验证。

4. 用户确认前不得将 Task 标记为最终完成。

5. 用户发现问题时，根据反馈修改，并重新执行自动验证 → 用户验证流程。

---

## 10. 完成标准

最终完成前必须确认：

- Backend 使用 `.venv/` 正常启动。
- Frontend 正常启动。
- 核心 API 实际调用成功。
- 核心用户流程端到端实际运行成功。
- Type Check、Lint 和必要测试通过。
- 无明显 Runtime Error。
- 删除 Debug Code 和无用代码。
- PRD / Task 与最终实现一致。
- `ARCHITECTURE.md` 与当前代码一致。
- 用户已完成最终手动验证。

最后只需简短总结：
- 实现了什么
- 如何启动
- 自动验证结果
- 关键技术决策
- 重要假设或已知限制
